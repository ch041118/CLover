# 단일 마이크·자동 배정 개편 검증 2026-09-25

- 서버 전체 테스트 141개 통과. 재시작 시 SQLite 날짜 비교 오류를 수정하고 만료 작업 회수와 동의 재확인을 검증했다.
- 앱 테스트 16개, TypeScript 검사, Android·iOS 번들 생성 통과. 앱 테스트 수는 기존 API·녹음 정리·음성 헬퍼 검사이며 새 화면의 실기기 조작 테스트 수가 아니다.
- 새 서버 시나리오: 대리 등록·기기 토큰/회수, 사전 동의 자동 확정, 근무 슬롯 분할, 지정 시각 불가 시 예외 전환, 중복 키/동시 접수 방어, 한 번 재질문, 기술 오류 상한, 취소·완료 통계, 담당 범위 권한, 거절 후 직접 조율과 전달 내용 변경.
- STT·LLM·지도는 테스트 대역을 사용했다. 실제 휴대폰 마이크·SecureStore·음성 재생·원격 푸시 도착·실제 모델 인식률과 지연·실제 네이버 경로·운영 PostgreSQL 다중 서버는 미검증.
- FLOW_PUSH_ENABLED 기본 false. Expo 접수 성공과 실제 기기 도착은 구분하며 receipt 확인은 미구현. 새 마이크 화면에는 기존 자동 안부 질문이 연결되지 않는다.
- 업데이트와 제한 사항: docs/FLOW-V2.md. 코딩 인계: docs/CODEX-HANDOFF.md.

# 지도 사용량과 선택적 재사용 검증 2026-09-23

- 관련 서버 테스트 15개, 모바일 테스트 16개와 TypeScript 검사 통과.
- 파일 기반 재시작 후 재사용·예산 유지, 암호화, 방향 구분, 만료 및 TTL 축소, 실패 미저장, 동시 중복 호출 억제, 재사용 비활성화 시 삭제, 주소 공백 정규화, 동의·통계 권한·한도 429 응답을 검증했다.
- 공개 네이버 Maps 약관상 결과 재사용 제한으로 MAPS_CACHE_ALLOWED 기본 false. 호출 건수 기록과 일일 상한은 기본 작동한다.
- 캐시 동시 중복 억제는 한 프로세스 기준. 여러 프로세스의 예산 예약은 SQLite 트랜잭션으로 처리하나 다중 서버·실기기·실제 네이버 청구/응답은 미검증.

# 주소 검색과 방문지 설정 개선 검증 2026-09-23

- 위도·경도 직접 입력을 없애고 검색 결과 선택 → 주소 저장으로 변경. 검색 진행·동의 필요·설정 필요·결과 없음·연결 실패를 구분해 표시한다.
- 주소명과 좌표 암호화 저장, 기존 2개 좌표 데이터 호환, 지도 설정 여부의 권한 및 키 미노출, 비정상 좌표/검색 응답 거부를 검사했다.
- 담당 팀·이동 경로 관련 서버 테스트 9개, 모바일 테스트 16개, TypeScript 검사와 Android 번들 생성 통과.
- 실제 네이버 API 키 호출과 실제 휴대폰 조작은 미검증. 이번 변경은 DB 스키마나 의존성을 변경하지 않는다.

# Android 녹음기 해제 오류 수정 검증 2026-09-23

- 음성 접수와 자동 음성 확인의 중단 처리에서 native stop의 동기 예외와 uri 접근 오류를 모두 처리한다. 중복 중단은 진행 중인 정리를 공유한다.
- 화면 이동 후 비동기 준비/종료 작업이 이어질 때 유효한 작업인지 확인한 뒤 녹음기를 사용한다.
- TypeScript 검사와 모바일 테스트 15개 통과. 새 회귀 테스트 4개는 해제된 getter/stop, 중단 도중 해제, 중복 정리, 파일 삭제 실패를 모의 재현한다.
- 실제 Android 기기에서 화면 이동·로그아웃·백그라운드 전환 확인은 남아 있다. 이번 수정에는 서버·DB·의존성 변경이 없다.

# 음성 접수 안내와 자동 음성 확인 검증 2026-09-21

- 서버 전체 114개, 앱 테스트 11개와 TypeScript 검사 통과.
- Android/iOS Expo 번들 생성 성공.
- 하루 응답 중복 방지, 5분 재질문과 하루 3회 제한, 다음 날 재개, 동의 철회·토큰 만료·권한·시간대 제한, 동시 시작 방지와 중단 검증.
- 응답 없음·음성 처리 오류·도움 요청을 구분하며, 기술 오류를 출석으로 기록하지 않음을 검증. 음성 인식은 모의 응답 사용.
- 실제 기기의 스피커·블루투스·무음 모드·마이크·배경 전환, 실제 음성 모델 인식률은 미검증. 앱 홈 화면의 전경 실행만 지원.

# 담당 팀과 지도 및 처리 이력 검증 2026-09-20

- 서버 전체 108개, 앱 테스트 11개와 TypeScript 검사 통과.
- Android/iOS Expo 번들 생성 성공. Expo 종료 지연 메시지 후 정상 종료 코드 확인.
- 담당 연결 관리자 권한, 다른 팀 배정 차단, 활성 일정 중 담당 변경 차단 검증.
- 관리자/담당 사회복지사 처리 이력 범위, 보고 복호화, 날짜 필터 검증.
- 모의 네이버 응답으로 출발지 이동 시간, 다음 방문 간격, 여유 시간, API 실패 시 차단, 주소 검색 동의, 좌표 암호화, 활성 일정의 위치 변경 제한 검증.
- 실제 네이버 API 키 호출, 경로 정확도, 미래 교통 상황, 실제 기기 조작과 운영 PostgreSQL 동시성은 미검증.

# AI 업무함과 전달문 승인 검증 2026-09-20

- 서버 전체 103개 테스트 통과, 모바일 테스트 11개와 TypeScript 검사 통과.
- Android/iOS Expo 번들 생성 성공. 이후 최대 20건 선택 안내 문구만 보완.
- 신규 검증: 접수 정리·담당 가져오기·묶음 승인·전달문 조회, 긴급/반복/동의 없음 예외, 상태 변경 시 전체 승인 거절, 비담당자 접근, 전달문 암호화, 원문에 없는 근거의 자동 제외.
- 기존 분류/개인정보/음성/일정 수락과 거절 테스트도 통과.
- 실제 모델 추론 품질·응답 시간, 실기기 화면과 마이크, 현장 업무시간 절감률, 운영 PostgreSQL 동시성은 미검증.

# 3단계 일정과 수락·거절 검증 2026-09-19

- 서버 전체 96개 통과 후, 중복 예약·다른 요양보호사 접근 검사를 추가해 일정 테스트 5개 재실행 통과(기존 4개 포함).
- 모바일 TypeScript 검사와 테스트 11개 통과.
- Android/iOS Expo 번들 생성 성공. 기기 설치·실기기 조작 검증은 아님.
- 자동 제안 재실행·동시 실행 중복 방지, 단계 구분, 긴급 요청의 확정 슬롯 침범 차단, 수락 전 시작 차단, 거절 사유 암호화, 재제안과 오래된 제안 차단, 취소 후 수락 차단, 수락 대기 일정의 이용자 중복 시간 차단을 검사.
- 실제 음성/텍스트 모델 추론, 현장 사용성, 운영 PostgreSQL 동시성은 미검증.

# 역할 분리·통계·반복 요청 검증 (2026-09-19)

- 백엔드 **92개**, 모바일 **11개** 자동 테스트 통과.
- TypeScript 타입 검사 및 Android/iOS Metro 번들 생성 통과.
- 사회복지사만 일정 확정/변경/배정, 배정된 요양보호사만 수행/보고하는 권한을 확인했습니다. 기존 요양보호사 accept/decline 경로의 우회도 차단합니다.
- 미검토 요청 배정 금지, 중복 배정 차단, 일정 관리 담당 동시 선점, 수행 기록 암호화와 비담당자 접근 차단을 확인했습니다.
- 100건을 넘는 요청의 전체 통계, 7일/3회 반복 요청 묶음, 중복 스캔, 후속 확인일, 검토 후 재발, 명시적으로 켠 자동 담당 배정과 종료 시 해제, 긴급 분류의 개별 검토 유지를 확인했습니다.
- 실기기 UI·실제 현장 운영·PostgreSQL 다중 서버 동시성은 미검증입니다. 단일 기관 전제이며 다기관 격리는 구현하지 않았습니다.

---

# 생활 돌봄·음성·일정 연결 검증 (2026-09-19)

- 백엔드: `python -m pytest -q` **85개 통과**.
- 모바일 통신·음성 확인 명령: `npm test` **11개 통과**.
- `npm run typecheck`: 통과.
- Android/iOS Metro 번들 생성: 모두 성공.
- 기존 백엔드 테스트에 더해 저장된 AI 동의/철회, 기본 안내문 대체, 지역/시간 필터, 동일 일정 동시 신청, 다른 요양보호사와 겹치는 신청, 수락/취소 권한, 지역 삭제 및 계정 중지, 음성 모델 미설치/시간 초과/비밀 문자열/전송 크기 제한을 검증했습니다.
- 음성 처리 테스트는 subprocess를 모의 실행했습니다. 실제 faster-whisper 모델 설치·한국어 인식률·마이크 권한·녹음 캐시 삭제·화면 배경 전환은 실기기 검증이 남아 있습니다.
- 실제 Ollama의 안내문 품질·저사양 PC 처리 시간은 미검증입니다. 사용자 PC에서 관찰된 기존 초안 오류의 정확한 원인은 로그가 없어 확정하지 않았습니다.
- SQLite 동시 예약 테스트를 통과했으며 운영 PostgreSQL/여러 서버 부하 검증은 별도로 필요합니다.
- 기존 의존성 경고는 남아 있습니다. 실기기 검증·APK/IPA 서명·앱스토어 배포는 하지 않았습니다.

이전 버전 검증 기록은 아래에 유지합니다.

---

# 모바일 앱 전환 검증 (2026-09-19)

- 백엔드 기존 테스트: **69개 통과**. 추가한 모바일 가입 승인 권한 테스트: **1개 통과**.
- 모바일 API 테스트: **9개 통과** (HTTPS/개발망 예외, 인증 만료, 로그아웃 중 응답 폐기, 자동 재전송 금지).
- `npm run typecheck`: 통과.
- `npx expo export --platform android --platform ios`: 두 플랫폼 번들 생성 성공.
- 검증 환경: Python 3.12, Node.js 24.19, Expo SDK 57.
- 백엔드 테스트에서 의존성 deprecation 경고 2건이 발생했으며 테스트 실패는 없습니다.

실기기 설치·화면/접근성 점검, 실제 Ollama 추론, APK/IPA 서명 및 스토어 배포는 미수행입니다.
요양보호사 일정 배정과 다기관 격리는 아직 미구현입니다.

---

# v3 로컬 전용 버전 검증

- Python 3.12 `python -m pytest -q`: **69 passed**, 테스트 의존성 deprecation warning 2건.
- `python -m compileall -q backend`: 성공.
- 모델: qwen2.5:0.5b 고정. 실제 가중치는 패키지에 미포함.

## 확인한 동작

- 모든 개인 돌봄 데이터는 내부 처리. 신원/건강/경제/상담 보호 태그와 인증정보 차단 유지.
- 일반 안내문도 같은 로컬 모델·loopback endpoint를 사용.
- 애플리케이션 AST와 requirements에서 boto3/botocore/anthropic/openai 의존성이 없음을 확인.
- Bedrock/hybrid/외부 모드 설정 거부, 원격 URL·cloud 모델·다른 모델 설정 거부.
- proxy 환경변수 무시, 리다이렉트·과대 응답·도구 호출·비정상 JSON·불완전 응답·timeout 차단.
- 긴 입력은 잘라서 추론하지 않고 내부 호출 전에 차단, 원문은 기존 API를 통해 암호화 저장 가능.
- 공유 추론 슬롯이 사용 중이면 두 번째 요청은 호출 없이 검토 대기/오류 처리.
- 일반 안내문 JSON, local consent, 역할 접근 통제, 기존 외부 동의 필드 거부.
- 기존 가입/승인/권한/암호화/담당자 배정/안부/패턴 테스트 유지.

## 아직 확인하지 않은 범위

- 실제 Ollama 설치 및 qwen2.5:0.5b 다운로드/추론. 테스트에서는 HTTP 모의 응답을 사용.
- 사용자 PC의 실행 RAM, CPU/GPU 성능, 한국어 판단·문장 품질.
- 전체 Next.js 앱 빌드, 운영 DB·네트워크 방화벽, 장비 로그·백업 보호.

`python check_local.py`는 설치된 실제 모델에 합성 문장만 보내 분류와 안내문 출력 형식을 확인합니다. 통과하더라도 분류 정확도 인증은 아닙니다. 모든 결과는 담당자가 확인해야 합니다.
